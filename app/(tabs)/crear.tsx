import React from "react";
import { PlaceholderScreen } from "../../components/PlaceholderScreen";

export default function CrearTramiteScreen() {
  return (
    <PlaceholderScreen
      icon="add-circle-outline"
      title="Crear trámite"
      description="Aquí construiremos el flujo de 12 pasos: tipo de trámite, ciudad, requisitos, documentos, gestor y pago."
    />
  );
}
